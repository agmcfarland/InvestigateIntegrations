% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repeat_class_summary_tables.R
\name{repeat_class_summary_tables}
\alias{repeat_class_summary_tables}
\title{Generate Summary Tables of Repeat Class Counts and Percentages}
\usage{
repeat_class_summary_tables(
  aavenger_unique_sites_table,
  mhc_repeats_counts_per_cluster
)
}
\arguments{
\item{aavenger_unique_sites_table}{A data frame containing unique site information with columns including \code{trial}, \code{subject}, \code{sample}, \code{posid}, and \code{repeat_class}.}

\item{mhc_repeats_counts_per_cluster}{A data frame containing counts of repeat classes for each multihit cluster, including columns such as \code{trial}, \code{subject}, \code{sample}, \code{clusterID}, \code{repeat_class}, and \code{repeat_class_count}.}
}
\value{
A named list of three data frames:
\describe{
\item{unique}{Summary table for unique repeat detections, with columns \code{trial}, \code{subject}, \code{sample}, \code{repeat_class}, \code{repeat_class_count}, \code{total_repeat_detections}, and \code{percentage_of_repeat_detections}.}
\item{multihits}{Summary table for multihit repeat detections, with similar columns as the unique table.}
\item{combined}{Combined summary table, consolidating unique and multihit repeat counts and percentages by repeat class for each sample.}
}
}
\description{
This function creates summary tables for repeat class counts and percentages, based on unique and multihit clusters, and combines them into a single table.
}
\details{
The function performs the following steps:
\enumerate{
\item Counts and calculates percentages for repeat classes in the \code{aavenger_unique_sites_table} (unique sites).
\item Counts and calculates percentages for repeat classes in the \code{mhc_repeats_counts_per_cluster} (multihit clusters).
\item Combines unique and multihit tables, recalculating the counts and percentages of repeat classes.
}
}
